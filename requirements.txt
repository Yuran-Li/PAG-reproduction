# Declared dependencies (install entry). Prefer docs/ENV.md for torch/vllm/flash-attn order.
# Known-good full snapshot: requirements.freeze.txt (reference only — do not blind-install).
# Then: pip install -e .
accelerate
codetiming
datasets
dill
flash-attn
hydra-core
liger-kernel
numpy
pandas
datasets
peft
pyarrow>=15.0.0
pybind11
pylatexenc
pylint==3.3.6
ray[default]
tensordict<=0.6.2
torchdata
transformers
# vllm==0.6.3.post1
wandb